Etter møte 31. oktober:

Her er litt feedback på koden som ble skrevet forrige møte. Dette er ment som konstruktiv kritikk, 
så ikke bli lei dere om det meste dere har gjort er kommentert på.


- Viewet for index burde kanskje hett archive?
- index pajer dersom databasen ikke inneholder noen artikler
- python bruker ikke camelcasing, vi bruker underscore.

    latestNews -> latest_news

- Dette er veldig java;
    i = 0
    for latest in latestNews:
        if (i % 2 == 0):
            latest.i = 0
        else:
            latest.i = 1
        i += 1

    Sjekk ut enumerate, dette gjør hele jobben til "i" i deres kode:
    
    for i, latest in enumerate(latestNews):
        
- render_to_response er litt utdatert -> sjekk render. Følgende kodelinjer gjør akkurat det samme:
    
    return render_to_response('article/index.html', {'featured' : featured[0], 'latest': latestNews}, context_instance=RequestContext(request))

    return render(request, 'article/index.html', {'featured' : featured[0], 'latest': latestNews})

- templates/article/index.html mangler?


